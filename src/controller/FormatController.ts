// --- Demo version. Code concealed ---
import { TabletFormat } from '@/formats/TabletFormat';
import { useFormat } from '@/model/FormatContext';
import { Dictionary, FormatType } from '@/model/Dictionary';
    case Dictionary.formats.tablet:
      return DesktopFormat;
}
export const FormatController = {
  resolve,
  return resolve(format);
// --- End of demo block ---
