// --- Demo version. Code concealed ---

    const {
      message,

    const userStr = user
    switch (level) {
        console.error(log);
        break;
        break;

// --- End of demo block ---
