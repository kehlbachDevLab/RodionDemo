// --- Demo version. Code concealed ---
import Blockbase from '@/view/components/Blockbase';
import BasicImage, {ImagePaths} from '@/view/components/BasicImage';





  const ref = useRef(null);
  const isInView = useInView(ref, { once: true, margin: '-100px' });

    <div id="wb_containerMain" ref={ref} className={formatStyles.wb_containerMain}>
        <div id="wb_twContainer" className={formatStyles.wb_twContainer}>
          <TypewriterManual
      </div>        
    </div>
// --- End of demo block ---
