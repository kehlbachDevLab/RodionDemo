// --- Demo version. Code concealed ---

type BlockbaseProps = {
  className?: string
export default function Blockbase({ id, displayText, className }: BlockbaseProps) {
  const formatStyles = useFormatController().components.blockbase;  
  return (
      <div
        }}
        ))}
// --- End of demo block ---
