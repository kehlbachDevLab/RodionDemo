// --- Demo version. Code concealed ---
import { motion } from 'framer-motion';


  const formatStyles = useFormatController().components.scrollToTopButton;
  useEffect(() => {
    if (!container) return;

    return () => container.removeEventListener('scroll', onScroll);

      whileHover={{ scale: 1.1 }}
      whileTap={{ scale: 0.95 }}
        }`}
// --- End of demo block ---
