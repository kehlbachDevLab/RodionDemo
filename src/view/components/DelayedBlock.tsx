// --- Demo version. Code concealed ---
type DelayedBlockProps = {
  textWaitingFor: string
  leftOffset: number

      animate={{ opacity: 1, x: leftOffset }}
      transition={{ delay: totalDuration, duration: 0.3 }} // totalDuration задается в секундах!
      {displayText}

export function calculateTypingDuration(
  const isArray = Array.isArray(text)
  const totalChars = isArray
  return totalChars * speedPerChar +  pause
// --- End of demo block ---
